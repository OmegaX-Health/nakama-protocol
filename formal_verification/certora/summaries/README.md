# Certora Solana Summaries

Certora's Solana prover currently requires Solana inlining and summary files.
The starter files in this directory are intentionally minimal placeholders so
the lane shape is committed before the first concrete CVLR rule is written.

Tighten these files together with each rule. Do not treat placeholder summaries
as proof coverage.
